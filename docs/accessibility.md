# Accessibility

- Colour schemes use sufficient contrast ratios (WCAG AA minimum)
- Charts include `aria-label` descriptions and keyboard navigation
- Interactive elements support screen reader announcements
- Focus indicators visible on all interactive elements
- All images have descriptive `alt` text
- Data tables include proper `<th>` scope attributes
- App supports reduced motion media query
